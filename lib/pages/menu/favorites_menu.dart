import 'package:flutter/material.dart';

class FavoriteMenu extends StatelessWidget {
  const FavoriteMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ini Favorite page"),
      ),
    );
  }
}
